image: node:latest
 
before_script:
    - npm install

cache:
  paths:
  - node_modules/ 
  
stages:
  - deploy

deploy:
  stage: deploy
  environment:
    name: Fiddle.be
    url: https://sitbe.fiddle.be
  script:
    - npm run lint
    - npm run build
    - npm run deploy --user=$FTP_USER --pwd=$FTP_PASS
  artifacts:
    paths:
      - node_modules/
  only:
    - master
