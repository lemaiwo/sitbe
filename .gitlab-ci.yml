image: node:latest

before_script:
  - npm install

cache:
  paths:
    - node_modules/
    # - dist/

# qa:
#   script:
#   only:
#     - merge_requests 

# build:
#   script:
#     - npm run build

publish:
  stage: deploy
  environment:
    name: dev.Fiddle.be
    url: https://dev.fiddle.be
  script:
    - echo "Publishing via FTP"
    - npm run deploy -- --dest=sitbe 
  only:
    - master
